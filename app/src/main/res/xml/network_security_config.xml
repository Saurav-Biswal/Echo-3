<?xml version="1.0" encoding="utf-8"?>
<!--
  Cleartext HTTP is a security risk and is disabled by default on Android.
  We re-enable it ONLY for the loopback hosts used during local development:
    - 10.0.2.2  : the host machine's localhost as seen from the emulator
    - localhost / 127.0.0.1 : on-device loopback
  Every other host still requires HTTPS. Before shipping to real users,
  point ECHO_BASE_URL at an https:// endpoint and delete this file.
-->
<network-security-config>
    <base-config cleartextTrafficPermitted="true" />
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="true">10.175.80.122</domain>
    </domain-config>
</network-security-config>
